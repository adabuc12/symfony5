{% extends 'base.html.twig' %}

{% block title %}OrderFactoryItem{% endblock %}

{% block body %}
    <h1>OrderFactoryItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_factory_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_factory_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_factory_item_index') }}">back to list</a>

    <a href="{{ path('order_factory_item_edit', {'id': order_factory_item.id}) }}">edit</a>

    {{ include('order_factory_item/_delete_form.html.twig') }}
{% endblock %}
