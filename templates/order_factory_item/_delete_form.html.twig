<form method="post" action="{{ path('order_factory_item_delete', {'id': order_factory_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_factory_item.id) }}">
    <button class="btn">Delete</button>
</form>
