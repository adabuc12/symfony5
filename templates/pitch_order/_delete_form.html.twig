<form method="post" action="{{ path('pitch_order_delete', {'id': pitch_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pitch_order.id) }}">
    <button class="btn">Delete</button>
</form>
