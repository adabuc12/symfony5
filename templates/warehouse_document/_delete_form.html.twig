<form method="post" action="{{ path('warehouse_document_delete', {'id': warehouse_document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ warehouse_document.id) }}">
    <button class="btn">Delete</button>
</form>
