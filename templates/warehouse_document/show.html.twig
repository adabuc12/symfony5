{% extends 'base.html.twig' %}

{% block title %}WarehouseDocument{% endblock %}

{% block body %}
    <h1>WarehouseDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warehouse_document.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ warehouse_document.type }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ warehouse_document.number }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ warehouse_document.date ? warehouse_document.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Is_brutto</th>
                <td>{{ warehouse_document.isBrutto ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('warehouse_document_index') }}">back to list</a>

    <a href="{{ path('warehouse_document_edit', {'id': warehouse_document.id}) }}">edit</a>

    {{ include('warehouse_document/_delete_form.html.twig') }}
{% endblock %}
