<form method="post" action="{{ path('kontrahent_delete', {'id': kontrahent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kontrahent.id) }}">
    <button class="btn">Delete</button>
</form>
