{% extends 'base.html.twig' %}

{% block title %}Factory{% endblock %}

{% block body %}
    <h1>Factory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ factory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ factory.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ factory.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ factory.email }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ factory.adress }}</td>
            </tr>
            <tr>
                <th>Pitch_transport_price</th>
                <td>{{ factory.pitchTransportPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('factory_index') }}">back to list</a>

    <a href="{{ path('factory_edit', {'id': factory.id}) }}">edit</a>

    {{ include('factory/_delete_form.html.twig') }}
{% endblock %}
